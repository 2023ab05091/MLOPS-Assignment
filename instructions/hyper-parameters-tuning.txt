# Documentation of the tuning process and best parameters found

Hyperparameter Tuning Process:
- Used GridSearchCV with 5-fold cross-validation to find the best hyperparameters for Logistic Regression.
- The parameter grid included:
  - solver: ['lbfgs', 'liblinear']
  - max_iter: [100, 200, 500, 1000]
  - multi_class: ['auto', 'ovr']
  - random_state: [8888]

Best Parameters Found:
- solver: {best_params['solver']}
- max_iter: {best_params['max_iter']}
- multi_class: {best_params['multi_class']}
- random_state: {best_params['random_state']}

Model Performance:
- Accuracy: {accuracy}
